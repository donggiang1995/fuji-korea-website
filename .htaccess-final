# LiteSpeed DirectoryIndex Fix
# This file fixes the exact issue: file exists but not auto-loading

# Set default index files in priority order
DirectoryIndex index.html index.htm default.html

# Disable directory browsing (prevent listing)
Options -Indexes

# LiteSpeed specific redirect for root access
RewriteEngine On

# If requesting root directory, redirect to index.html 
# This forces LiteSpeed to load the file
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^$ /index.html [L,R=301]

# Additional fallback for empty requests
RewriteCond %{REQUEST_FILENAME} -d
RewriteCond %{REQUEST_FILENAME}/index.html -f
RewriteRule ^(.*)$ $1/index.html [L]

# Error document fallback
ErrorDocument 404 /index.html
ErrorDocument 403 /index.html

# Security headers
Header always set X-Content-Type-Options nosniff
Header always set X-Frame-Options SAMEORIGIN